// Template for local secrets. Copy this file to Secrets.xcconfig and fill in the values.
// Secrets.xcconfig is gitignored and must NOT be committed.

// Yandex MapKit API key (same key as Android local.properties: yandex.map.api.key).
YANDEX_MAP_API_KEY =
